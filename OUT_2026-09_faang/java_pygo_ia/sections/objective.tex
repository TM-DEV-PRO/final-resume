\begin{zitemize}
    \item Senior backend engineer, \textbf{5 years} building and owning \textbf{cloud-native distributed systems} in \textbf{Java, Python, and Go} on \textbf{GCP} and \textbf{AWS}.
    \item Currently owns both the \textbf{Go API service} and the \textbf{LLM decision engine} behind a multi-tenant retail planning platform --- deciding which of \textbf{88,863} products a retailer keeps each season, over a \textbf{2.11B-row ClickHouse} fact table, with \textbf{agents held SELECT-only}.
    \item Ships and then hardens: \textbf{Spring Boot}/FastAPI microservices, \textbf{100\% coverage CI gates}, \textbf{Kafka/Flink} ingestion at \textbf{30K+ menus/month}, and migrations that cut \textbf{p95 latency 4x}.
\end{zitemize}
